

use int.Int

val ref x : int

let f () : int
  ensures { result < 1 }
= x <- x+1;
  x+2

let g () : int
  returns { r -> r < 1 }
= 1+4


let h () =
  let t = f ()  in
  assert { t  = 0 }
  
  
let i () =
  let u = f () + 1 in
  assert { u  = 0 }